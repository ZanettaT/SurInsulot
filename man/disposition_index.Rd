% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disposition.R
\name{disposition_index}
\alias{disposition_index}
\title{Disposition index}
\usage{
disposition_index(secretion, sensitivity)
}
\arguments{
\item{secretion}{A secretion surrogate (for example \code{\link[=igi]{igi()}} or \code{\link[=cpi]{cpi()}}).}

\item{sensitivity}{A sensitivity surrogate where higher means more sensitive
(for example \code{\link[=matsuda]{matsuda()}}, \code{\link[=quicki]{quicki()}}, or \code{1 / homa_ir()}).}
}
\value{
A numeric vector of disposition-index values.
}
\description{
The (oral) disposition index expresses insulin secretion relative to the
prevailing insulin sensitivity as their product: a given amount of secretion
means healthier beta cells if it occurs in an insulin-sensitive person.
\code{disposition_index()} is simply
\deqn{DI = secretion \times sensitivity}
and every named variant in the literature is this product with a particular
pair of surrogates plugged in.
}
\details{
\tabular{ll}{
oral DI (oDI)      \tab \code{igi()}\eqn{_{30}} x \verb{1 / fasting insulin} \cr
oDI (Matsuda)      \tab \code{igi()}\eqn{_{30}} x \code{matsuda()} \cr
oDI (QUICKI)       \tab \code{igi()}\eqn{_{30}} x \code{quicki()} \cr
oDI (1/HOMA-IR)    \tab \code{igi()}\eqn{_{30}} x \code{1 / homa_ir()} \cr
oDI (C-peptide)    \tab \code{cpi()}\eqn{_{30}} x \code{matsuda()} \cr
ISSI-2             \tab (AUC insulin:glucose, 0-120) x \code{matsuda()}
}

Pass a secretion index (for example the insulinogenic index \code{\link[=igi]{igi()}}) and a
\emph{sensitivity} index (higher = more sensitive, for example \code{\link[=matsuda]{matsuda()}},
\code{\link[=quicki]{quicki()}}, or \code{1 / homa_ir()}). Do not pass a resistance index such as
HOMA-IR directly; invert it first.
}
\examples{
ig  <- igi(insulin_0 = 10, insulin_t = 80, glucose_0 = 5.3, glucose_t = 8.9)
mat <- matsuda(glucose_0 = 95, insulin_0 = 10,
               glucose_mean = 140, insulin_mean = 60)
disposition_index(secretion = ig, sensitivity = mat)   # oDI (Matsuda)

}
\references{
Utzschneider KM et al. (2009) oral disposition index.
\emph{Diabetes Care} 32:335-341.
Retnakaran R et al. (2009) ISSI-2. \emph{Obesity} 17:1232-1238.
}
