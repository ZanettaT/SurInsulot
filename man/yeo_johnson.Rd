% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform-yj.R
\name{yeo_johnson}
\alias{yeo_johnson}
\alias{yeo_johnson_lambda}
\title{Yeo-Johnson power transform}
\usage{
yeo_johnson(x, lambda)

yeo_johnson_lambda(x, interval = c(-5, 5))
}
\arguments{
\item{x}{A numeric vector.}

\item{lambda}{The power parameter (for example as returned by
\code{yeo_johnson_lambda()}).}

\item{interval}{Search interval for \code{lambda} (default \code{c(-5, 5)}).}
}
\value{
\code{yeo_johnson()} returns the transformed vector, the same length as
\code{x}. \code{yeo_johnson_lambda()} returns the single fitted \code{lambda}, or \code{NA} when
\code{x} has fewer than 10 finite values or zero variance.
}
\description{
The Yeo-Johnson transform normalises a numeric vector while remaining defined
for zero and negative values (unlike a log or Box-Cox). \code{yeo_johnson()}
applies the transform for a given power parameter \code{lambda};
\code{yeo_johnson_lambda()} estimates \code{lambda} by maximum likelihood - the value
that makes the transformed vector most nearly Gaussian. This is the transform
behind the \verb{*_yj} columns in the GRADE/DPP reports and the \code{transform = TRUE}
option of \code{\link[=compute_surrogates]{compute_surrogates()}}.
}
\details{
For \eqn{x \ge 0}: \eqn{((x+1)^\lambda - 1)/\lambda}, or \eqn{\ln(x+1)} when
\eqn{\lambda = 0}. For \eqn{x < 0}:
\eqn{-((-x+1)^{2-\lambda} - 1)/(2-\lambda)}, or \eqn{-\ln(-x+1)} when
\eqn{\lambda = 2}. \eqn{\lambda = 1} is the identity. Non-finite values pass
through unchanged. \code{lambda} is fitted over all finite values; refit it on each
training fold under cross-validation to avoid leakage.
}
\examples{
x <- c(1, 2, 2, 3, 5, 8, 13, 21, 34, 55)   # right-skewed
lam <- yeo_johnson_lambda(x)
yeo_johnson(x, lam)
yeo_johnson(x, 1)                           # lambda = 1 is the identity

}
\references{
Yeo IK, Johnson RA (2000) A new family of power transformations to improve
normality or symmetry. \emph{Biometrika} 87:954-959.
}
