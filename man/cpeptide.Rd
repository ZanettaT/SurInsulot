% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpeptide.R
\name{cpeptide}
\alias{cpeptide}
\alias{cpeptide_glucose_ratio}
\alias{cpi}
\alias{insulin_cpeptide_ratio}
\alias{cpeptide_insulin_auc_ratio}
\title{C-peptide secretion and hepatic-extraction indices}
\usage{
cpeptide_glucose_ratio(cpeptide_t, glucose_t)

cpi(cpeptide_0, cpeptide_t, glucose_0, glucose_t)

insulin_cpeptide_ratio(insulin_0, cpeptide_0)

cpeptide_insulin_auc_ratio(auc_cpeptide, auc_insulin)
}
\arguments{
\item{cpeptide_0, cpeptide_t}{Fasting and post-load C-peptide, nmol/L.}

\item{glucose_0, glucose_t}{Fasting and post-load glucose, mmol/L.}

\item{insulin_0}{Fasting insulin, pmol/L (for \code{insulin_cpeptide_ratio()}).}

\item{auc_cpeptide}{Trapezoidal C-peptide area, nmol/L*min.}

\item{auc_insulin}{Trapezoidal insulin area, uU/mL*min.}
}
\value{
A numeric vector the length of the recycled inputs.
}
\description{
C-peptide is co-secreted with insulin but escapes hepatic first-pass
extraction, so C-peptide indices track beta-cell output, and insulin:C-peptide
ratios track hepatic insulin extraction.
}
\details{
\tabular{ll}{
\code{cpeptide_glucose_ratio()} \tab \eqn{CP_t / G_t} (CP nmol/L, G mmol/L) \cr
\code{cpi()}                     \tab \eqn{(CP_t - CP_0) / (G_t - G_0)} \cr
\code{insulin_cpeptide_ratio()}  \tab \eqn{I_0 / (CP_0 \times 1000)} (molar) \cr
\code{cpeptide_insulin_auc_ratio()} \tab \eqn{(AUC_{CP} \times 1000) / (AUC_I \times 6.945)}
}

\code{cpeptide_glucose_ratio()} and \code{cpi()} (the C-peptide analogue of the
insulinogenic index) are timepoint-agnostic. \code{insulin_cpeptide_ratio()}
expects \strong{fasting insulin in pmol/L} and \strong{C-peptide in nmol/L} and returns
the dimensionless molar ratio: C-peptide is scaled by 1000 to pmol/L before
dividing, giving values around 0.1-0.2 in health (insulin and C-peptide are
co-secreted, but C-peptide is cleared more slowly so its molar concentration
is higher). \code{cpeptide_insulin_auc_ratio()} converts the C-peptide area
(nmol/L\emph{min) and insulin area (uU/mL}min) to a molar output ratio.
}
\note{
The GRADE analysis script computes this ratio as
\code{(insulin_pmol / cpeptide_nmol) * 1000}, which is 10^6 times larger than a
true molar ratio (the 1000 factor is on the wrong side). Because it is
rank-preserving this does not change the Spearman analyses in that report,
but SurInsulot returns the corrected molar ratio. To reproduce the script's
raw values exactly, multiply this result by \code{1e6}.
}
\examples{
cpeptide_glucose_ratio(cpeptide_t = 1.2, glucose_t = 8.5)
cpi(cpeptide_0 = 0.6, cpeptide_t = 1.8, glucose_0 = 5.3, glucose_t = 8.9)
insulin_cpeptide_ratio(insulin_0 = 70, cpeptide_0 = 0.6)

}
\references{
Polonsky KS, Rubenstein AH (1984) \emph{Diabetes} 33:486-494 (C-peptide kinetics).
}
