% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ogtt-sensitivity.R
\name{matsuda}
\alias{matsuda}
\alias{matsuda_from_curve}
\title{Matsuda whole-body insulin-sensitivity index}
\usage{
matsuda(glucose_0, insulin_0, glucose_mean, insulin_mean)

matsuda_from_curve(times, glucose, insulin, upto = NULL)
}
\arguments{
\item{glucose_0}{Fasting (0 min) glucose, mg/dL.}

\item{insulin_0}{Fasting (0 min) insulin, uU/mL.}

\item{glucose_mean, insulin_mean}{Mean glucose (mg/dL) and insulin (uU/mL)
across the OGTT window.}

\item{times}{Numeric vector of OGTT sampling times in minutes, starting at 0.}

\item{glucose, insulin}{Numeric matrices (subjects in rows, timepoints in
columns) or single numeric vectors, aligned to \code{times}. Glucose mg/dL,
insulin uU/mL.}

\item{upto}{Optional upper time bound (minutes). Timepoints at or before
\code{upto} are used for the mean; \code{NULL} (default) uses the whole curve.}
}
\value{
A numeric vector of Matsuda index values.
}
\description{
The Matsuda index summarises whole-body insulin sensitivity from an OGTT.
\code{matsuda()} is the faithful formula given the fasting values and the mean
glucose and insulin across the curve:
\deqn{10000 / \sqrt{G_0 \times I_0 \times \bar{G} \times \bar{I}}.}
\code{matsuda_from_curve()} is a convenience wrapper that computes the means for
you as trapezoidal time-averages over the sampled window, reproducing the
"modified Matsuda" variants (0-15, 0-30, 0-60, 0-90) used when only part of
the curve is available.
}
\details{
Glucose is in \strong{mg/dL} and insulin in \strong{uU/mL}. The mean is the
trapezoidal area under the curve divided by the covered time span, matching
the reference implementation (this is the "modified" Matsuda mean rather than
the simple arithmetic mean of the original paper).
}
\examples{
matsuda(glucose_0 = 95, insulin_0 = 10,
        glucose_mean = 140, insulin_mean = 60)

g <- c(95, 160, 150, 130, 110)
i <- c(10, 80, 90, 70, 45)
matsuda_from_curve(c(0, 30, 60, 90, 120), g, i)          # full
matsuda_from_curve(c(0, 30, 60, 90, 120), g, i, upto = 30) # modified 0-30

}
\references{
Matsuda M, DeFronzo RA (1999) \emph{Diabetes Care} 22:1462-1470.
}
