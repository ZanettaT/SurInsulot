% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-auc.R
\name{guard_ratio}
\alias{guard_ratio}
\title{Guarded ratio}
\usage{
guard_ratio(num, den, min_den = 0)
}
\arguments{
\item{num, den}{Numeric vectors, the numerator and denominator.}

\item{min_den}{Numeric scalar; the ratio is computed only where
\code{den > min_den}. Defaults to \code{0}.}
}
\value{
A numeric vector of the same length as \code{num} / \code{den}, with \code{NA}
where the guard fails.
}
\description{
Divide \code{num} by \code{den}, returning \code{NA} wherever the denominator is not
strictly greater than \code{min_den}. This protects ratio indices (for example
incremental AUC ratios) from dividing by zero or by a non-positive
increment.
}
\examples{
guard_ratio(c(10, 20, 5), c(2, 0, -1))   # 5, NA, NA

}
