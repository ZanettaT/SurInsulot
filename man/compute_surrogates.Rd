% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-surrogates.R
\name{compute_surrogates}
\alias{compute_surrogates}
\title{Compute every estimable surrogate index from a data frame}
\usage{
compute_surrogates(
  data,
  glucose_unit = c("mg/dL", "mmol/L"),
  insulin_unit = c("uU/mL", "pmol/L"),
  cpeptide_unit = c("nmol/L", "ng/mL", "pmol/L"),
  trig_unit = c("mg/dL", "mmol/L"),
  hdl_unit = c("mg/dL", "mmol/L"),
  glucagon_unit = c("pmol/L", "pg/mL"),
  sex_col = "sex",
  male_code = 1,
  verbose = TRUE,
  transform = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per observation (for example one
participant-visit) and the columns above.}

\item{glucose_unit}{Unit of the \verb{g*} columns: \code{"mg/dL"} (default) or \code{"mmol/L"}.}

\item{insulin_unit}{Unit of the \verb{i*} columns: \code{"uU/mL"} (default) or \code{"pmol/L"}.}

\item{cpeptide_unit}{Unit of the \verb{cp*} columns: \code{"nmol/L"} (default),
\code{"ng/mL"} (divided by 3.02), or \code{"pmol/L"} (divided by 1000).}

\item{trig_unit}{Unit of \code{trig}: \code{"mg/dL"} (default) or \code{"mmol/L"}.}

\item{hdl_unit}{Unit of \code{chdl}: \code{"mg/dL"} (default) or \code{"mmol/L"}.}

\item{glucagon_unit}{Unit of \verb{glcg*}: \code{"pmol/L"} (default) or \code{"pg/mL"}
(divided by 3.483, glucagon molar mass ~3483 g/mol).}

\item{sex_col}{Name of the column holding sex (default \code{"sex"}). Used only by
the sex-specific indices (VAI, LAP, METS-VF, BIGTT).}

\item{male_code}{The value of \code{sex_col} that denotes men (default \code{1}). Men
are encoded as 1 and everyone else as 0 for the sex-specific indices.}

\item{verbose}{If \code{TRUE} (default), message how many index columns were
computed.}

\item{transform}{If \code{TRUE}, also append a Yeo-Johnson-normalised
\verb{<index>_yj} column for every computed index (lambda fitted per index by
maximum likelihood over the supplied rows), reproducing the \verb{*_yj} outputs
of the GRADE/DPP reports. The fitted lambdas are stored in
\code{attr(result, "surinsulot_yj_lambda")}. Default \code{FALSE}.}
}
\value{
\code{data} with one appended column per computed index (names match the
GRADE/DPP scripts, for example \code{homa_ir}, \code{matsuda}, \code{igi_30}, \code{ogis}), and
an extra \verb{<index>_yj} column per index when \code{transform = TRUE}. The names of
the computed indices are stored in \code{attr(result, "surinsulot_computed")}.
}
\description{
\code{compute_surrogates()} derives the full panel of insulin sensitivity /
resistance and secretion surrogates from a data frame of fasting and OGTT
measurements, reproducing the GRADE and DPP analysis pipelines. It computes
each index only when its required inputs are present as columns, so the same
call works on a rich OGTT dataset or on a fasting-only dataset - indices that
cannot be computed are simply skipped.
}
\details{
HOMA2 indices (\code{homa2_ir}, \code{homa2_b_ins}, \code{homa2_s}, and the C-peptide
variants \code{homa2_ir_cp}, \code{homa2_b_cp}, \code{homa2_s_cp}) are computed with the
built-in \code{\link[=homa2_insulin]{homa2_insulin()}} and \code{\link[=homa2_cpeptide]{homa2_cpeptide()}} functions (Oxford DTU
reference tables); fasting insulin is converted to pmol/L at 6.945 before the
table lookup.
}
\section{Expected columns}{

Supply any subset of the following, using these names and units. Missing
columns just switch off the indices that need them.
\tabular{lll}{
\strong{Column} \tab \strong{Meaning} \tab \strong{Unit} \cr
\verb{g000,g015,g030,g060,g090,g120} \tab glucose at 0-120 min \tab mg/dL \cr
\verb{i000,i015,i030,i060,i090,i120} \tab insulin at 0-120 min \tab uU/mL \cr
\verb{cp000,cp015,cp030,cp060,cp090,cp120} \tab C-peptide at 0-120 min \tab nmol/L \cr
\verb{glcg000,glcg030} \tab glucagon at 0 / 30 min \tab pmol/L \cr
\verb{trig,chdl} \tab triglycerides, HDL cholesterol \tab mg/dL \cr
\code{hba1c_perc} \tab HbA1c \tab percent \cr
\verb{adipon,leptin,pin} \tab adiponectin, leptin, fasting proinsulin \tab ug/mL, ng/mL, pmol/L \cr
\verb{bmi,weight_kg,waist_cm,hip_cm,height_cm,whr,whtr} \tab anthropometry \tab kg/m^2, kg, cm, cm, cm, ratio, ratio \cr
\verb{sex,age,hypertension} \tab covariates \tab see \code{sex_col}/\code{male_code}; years; 1/0 \cr
}
The units in the table are the defaults. If your data are in other units,
declare them with the \verb{*_unit} arguments and they are converted internally
first - e.g. \code{glucose_unit = "mmol/L"}, \code{insulin_unit = "pmol/L"},
\code{cpeptide_unit = "ng/mL"}. Everything is normalised to the canonical units
above before any index is computed, and glucose/insulin are then converted to
mmol/L / pmol/L for the indices that need them.
}

\examples{
df <- data.frame(
  g000 = c(95, 110), g030 = c(160, 190), g120 = c(140, 170),
  i000 = c(10, 18),  i030 = c(80, 60),
  trig = c(150, 200), chdl = c(45, 38), bmi = c(29, 34),
  sex = c(1, 2)
)
out <- compute_surrogates(df)
out[, c("homa_ir", "quicki", "tyg", "igi_30")]

}
\seealso{
The individual index functions, e.g. \code{\link[=homa_ir]{homa_ir()}}, \code{\link[=matsuda]{matsuda()}},
\code{\link[=igi]{igi()}}, \code{\link[=ogis]{ogis()}}.
}
